<Window x:Class="Lafarge_WPF.Progress_bar"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lafarge_WPF"
        mc:Ignorable="d"
        AllowsTransparency="True"
        Background="Transparent"
        Title="Progress_bar" Height="{x:Static SystemParameters.PrimaryScreenHeight}" 
        Width="{x:Static SystemParameters.PrimaryScreenWidth}">
    
    <Grid Background="Transparent" >
        <ProgressBar x:Name="progress" Foreground="Green" 
                     Background="Gray"  Width="700" Height="50">
            <ProgressBar.Effect>
                <DropShadowEffect Color="Gray" BlurRadius="15" Direction="-90" RenderingBias="Quality" ShadowDepth="4" Opacity="0.30" />
            </ProgressBar.Effect>
        </ProgressBar>
        
        <TextBlock x:Name="ProgressTextblock" FontSize="25" FontWeight="SemiBold" Foreground="White" 
                   VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0 135 0 0" />
        <Grid Background="WhiteSmoke" Opacity="0.1">
            
        </Grid>
        
    </Grid>
</Window>
